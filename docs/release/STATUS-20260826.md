# Release-candidate status — 2026-08-26

## Current decision

`GO_LIVE=NO_GO`

## Implemented on the release branch

- crawl-job worker leases, heartbeat, stale-lease recovery and write fencing;
- outbox claimant tokens and stale-claim acknowledgement rejection;
- deterministic BullMQ dispatch by database job ID and version;
- real PostgreSQL and Redis integration tests;
- rollback/re-application tests for migrations `002` and `003`;
- signed duplicate/replay transport tests for n8n event and Odoo-result paths;
- Kong DB-less routes, request-size limits, rate limits and private-route allowlist;
- Caddy TLS edge and mTLS integration hostname;
- functional gateway security validation workflow;
- immutable GHCR image publication with SBOM and provenance;
- write-disabled staging deployment and no-write crawl canary;
- staging rollback and reviewed-candidate restoration workflow;
- explicit production approval template.

## Evidence still required before GO

- a green final CI run on the unchanged release SHA;
- a green gateway validation run on the unchanged release SHA;
- the final GHCR image digest and provenance artifact;
- an independent human approval of the unchanged source SHA;
- configured staging environment secrets and an authorized staging host;
- successful staging deployment/canary evidence;
- successful rollback-and-restore evidence;
- verified production DNS, certificates, secrets and host inventory;
- explicit production approval naming both the source SHA and image digest.

No source file or workflow result may be described as a live deployment without
the corresponding staging or production deployment evidence.
